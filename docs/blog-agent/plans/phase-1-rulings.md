# Phase 1 — every ruling made during execution (exported from the ledger)

7:- Ruling: No git anywhere (spec Rule 10) — "commits" are replaced by snapshot labels (snapshots/task-N-base, task-N-rK) and diffs come from package.sh — costs nothing if wrong; the reviewed content is identical.
8:- Ruling: WAITING_FOR_TOPIC -> FAILED edge NOT added (Task 4 Step 0 "no answer yet" path) — owner did not answer; nothing in Phase 1 reaches that state — if wrong, Phase 3 adds one transition and three test-count lines.
9:- Ruling: Daily runs get the same workflow timeout as manual runs (production_timeout_minutes*60, via SetWorkflowTimeout around the child start in daily_trigger, Task 11) — owner did not object to the proposal — if wrong, remove one context manager.
10:- Ruling: Settings drops validate_by_name=True (keeps validate_by_alias=True) so generic env vars named like fields (TIMEZONE, MOCK_MODE, PUBLISHER) are never read (Task 2); tests that build Settings by python field name switch to aliases/model_copy — owner did not object — if wrong, re-add one config key.
11:- Ruling: Task 13 does not lift the ECC pre:config-protection hook. The Vite scaffold (same verified command) is generated into a temporary directory inside frontend's parent, all files except eslint.config.js are copied into frontend/, then frontend/eslint.config.js is created once (first-time creation, which the hook allows by design) with the plan's final content — the file's content is exactly the plan's, reviewed in the Task 13 diff; the owner is told — if the owner objects, delete and recreate under the plan's original owner gate (same content).
12:- Ruling: Models — implementers and reviewers: sonnet (plan gives complete code but each task is multi-step Docker work, so mid-tier is the floor); fix rounds 4-5 and adjudication: opus; final whole-branch review: opus — cost only.
13:- Ruling: Subagents must never run host python/node/uv/npm, any git command, or any docker prune / host-wide cleanup; they remove only Docker resources they created, by name — incident on 2026-09-17 during planning.
18:- Ruling: C1 worker stop_grace_period = 60s (spec §3) in Task 11 text/comments, Task 14 compose and Step 13c; still > 25s shutdown wait — cost: none.
19:- Ruling: C2 Task 9 `_client_ip` comment/prose must say the api runs with --no-proxy-headers (Task 14), not a pinned web IP; code unchanged — cost: none.
20:- Ruling: C3 Task 1 Step 1 expected listing includes `.superpowers/` (controller workspace, git-ignored) — cost: none.
21:- Ruling: C4 Task 14 fallback Dockerfile comment reworded so it does not contain the literal `--no-editable` checked by Step 6 — cost: none.
22:- Ruling: C5 Task 14 Step 8 text: db block differs from Task 1 only by healthcheck timings — cost: none.
23:- Ruling: C6 Task 14 Steps 13a–13i doc edits proceed, but every spec-behaviour change (readyz scope, /me→/session, migrate command, code layout; grace period stays 60s so no change) is listed for owner sign-off in the final report — cost: owner may ask to revert doc wording.
24:- Ruling: A1 acceptance secret check fails the run if the literal configured provider key values (read from .env inside the script, never echoed; skipped when empty) appear in api/worker logs; the broad `sk-|AIza` pattern stays a warning because `sk-` matches ordinary text like "task-" — cost: a key rendered in a transformed form would be missed by the literal check (pattern warning still shows it).
25:- Ruling: A2 no global InvalidTransition→409 handler in Phase 1 (Task 12 maps it locally; no other Phase 1 route can raise it) — deferred to Phase 4/5 when article routes arrive — cost: a stray InvalidTransition would be a 500 until then.
29:- Task 1: parked — [0] runtime outputs in the report (compose config/build, db health, db env names, ruff/mypy) were not re-run by the reviewer — Ruling: no gap; adjudicator re-checked read-only: `compose config --quiet` ok and services db,tools; db container env names are only POSTGRES_DB/USER/PASSWORD plus image defaults (docker inspect, names only), TCP healthcheck, digest-pinned image, 127.0.0.1:5440; image has editable install (.pth -> /app/src, mdcopilot-blog-backend 0.1.0); all 12 files byte-match the brief; uv.lock 84 packages; google-genai 2.24.0 vs plan's informational 2.23.0 is not pinned by the spec or gated by the brief — cost if wrong: none, the evidence is direct.
30:- Task 1: parked — [1] mypy strict / build zero-warnings (uuid_utils typing) taken on the report's word — Ruling: no gap; uuid_utils 1.0.0 ships py.typed and __init__.pyi in the image, and a throwaway no-network container with read-only mounts gave "All checks passed!", "6 files already formatted", "Success: no issues found in 2 source files" and "4 passed" with -W error — cost if wrong: none; Task 2 re-runs the same gates.
33:Task 2: parked — [0] the real .env passing every validator was taken on the report's word — Ruling: no gap; adjudicator re-ran the brief's Step 5 in the tools container (--rm --no-deps, PYTHONDONTWRITEBYTECODE=1, prints only non-secret fields and booleans, no .env values shown) and got the exact expected line `settings ok development True False manual_export [9 route keys]`, plus True for: POSTGRES_PASSWORD has no whitespace, dbos_system_database_url round-trips the password through make_url, SESSION_SECRET length >= 32; human_approval_required True, publishing_enabled False — cost if wrong: none, the evidence is direct; a later .env edit is re-checked by Task 14's acceptance run.
34:Task 2: parked — [1] WORKER_EXECUTOR_ID is a Settings field but absent from .env.example — Ruling: intended by the plan; the spec (ARCHITECTURE worker row, IMPLEMENTATION_PLAN Phase 1) only requires a fixed executor_id and does not say where it is set; the Task 2 field matches the plan exactly (default worker-1); Task 14 sets `WORKER_EXECUTOR_ID: worker-1` in the worker's compose environment, and its Step 3 env-coverage check lists the variable as `compose_only`; .env.example is outside Task 2's file list — cost if wrong: one line added to .env.example in Task 14.
35:Task 2: parked — [2] review diff header lists `frontend/package-lock.json changed or added` — Ruling: false positive from the controller's package.sh, not a Task 2 change; no snapshot has a frontend/ tree, `diff -rq task-2-base task-2-r0` lists only settings.py and test_settings.py, and `cmp -s` exits 2 when both files are missing (verified), which the `if !` test reads as changed (the same line is in review-task-1-r0.diff); expect it on every package until Task 13 creates the lock file; the controller may add `[ -e "$base/$f" ] || [ -e "$head/$f" ] || continue` to that loop — cost if wrong: none; the file list comes from diff -rq.
39:Task 3: parked — [0] pytest-asyncio config (asyncio_mode=auto, session loop scope) lives in Task 1's pyproject.toml, outside the diff — Ruling: no gap; backend/pyproject.toml [tool.pytest] sets asyncio_mode="auto" and both asyncio_default_fixture_loop_scope and asyncio_default_test_loop_scope to "session", and a throwaway container (no network, read-only mount) printed the header "asyncio: mode=Mode.AUTO, asyncio_default_fixture_loop_scope=session, asyncio_default_test_loop_scope=session" with pytest-asyncio 1.4.0, and all 24 Task 3 tests passed — cost if wrong: none, the evidence is direct.
40:Task 3: parked — [1] the report's full-suite numbers (75 passed; ruff/mypy clean on 5 source files) were not re-run by the reviewer — Ruling: no gap; adjudicator re-ran the brief's Step 9 in a throwaway container (no network, read-only backend mount, no bytecode or cache writes) and got "75 passed" (75 collected, also clean under -W error), "All checks passed!", "13 files already formatted", "Success: no issues found in 5 source files"; all 5 delivered files byte-match the brief's code blocks (cmp) — cost if wrong: none; later tasks re-run the same gates.
41:Task 3: parked — [2] runtime import of pydantic Secret and mypy-strict acceptance of isinstance(value, SecretStr | SecretBytes | Secret) taken on the report's word — Ruling: no gap; in the image (pydantic 2.13.5, fastapi 0.141.1, starlette 1.6.0) mypy strict with the pydantic plugin passes on logs.py, and a runtime check of a model field typed Secret[int] (as %s and %r message args and as extra, including inside a nested tuple alongside a SecretStr) leaked neither value and wrote the mask 5 times; the union isinstance check evaluates correctly — cost if wrong: none; spec §security "never logged" is met for all three secret types.
45:Task 4: parked — [0] TRANSITIONS and ROLE_PERMISSIONS were checked only against the brief's copy, not against ARCHITECTURE §6/§17 — Ruling: no gap; adjudicator compared the code with the spec file directly (throwaway container, no network, read-only mounts): the §17 table parsed from ARCHITECTURE.md equals ROLE_PERMISSIONS (28 grants; Role and Permission enums match its columns and rows); the status names in the §6 run, article and publication diagrams equal the enum values; every edge the §6 diagrams name is present; the 28 extra edges each trace to spec text (§5.5 restart/cancel/timeout, §5.3 regenerate_topics/recheck/regenerate/change_topic, §5.2 P10 fix pass, §6+§11 an edited version needs fresh approval, publish retry) and none reaches EXPORTED/PUBLISHING/PUBLISHED without APPROVED; QUALITY_GATE_FAILED→APPROVED is the known §24.2 pre-emption (advisory A4, Phase 5); §6 is a diagram, so a faithful expansion is what the plan requires, not a verbatim copy; the six §12 models' field lists match the spec in order — cost if wrong: a later phase edits one table line and its test counts; no Phase 1 route or workflow uses a disputed edge.
46:Task 4: parked — [1] pydantic 2.13.5 camelCase, strictness and JSON-schema behaviour taken on the report's word — Ruling: no gap; adjudicator re-ran the brief's gates in a throwaway read-only container: 727 passed (also clean under -W error), "All checks passed!", "21 files already formatted", "no issues found in 10 source files", test files "no issues found in 3 source files", pydantic 2.13.5; a probe also showed a stable camelCase JSON and dict round-trip for GeneratedBlogPost, a snake_case dump that parses back, camelCase keys + every field required + additionalProperties=false in every $defs entry of both validation and serialization schemas for all 21 models, and unknown keys rejected inside a dict value and inside a nested list item; observation only: lax mode accepts "0.5" and True for a UnitScore (pydantic default; neither spec nor brief asks for strict mode) — cost if wrong: none, the evidence is direct.
47:Task 4: parked — [2] the owner never answered Step 0 (WAITING_FOR_TOPIC→FAILED) — Ruling: already settled by the controller's pre-flight ruling (preflight §3.4, ledger pre-flight line 2); the code has the one-line "no" form (WAITING_FOR_TOPIC → TOPICS_READY, PRODUCING, CANCELLED only), the report records "not added", and the Task 14 Step 13j greps print 0 and 0 as §3.4 requires; the §6 run line does not require the edge (read literally it would also allow WAITING_FOR_TOPIC→SUCCEEDED, which the plan tests as illegal), and nothing in Phase 1 enters WAITING_FOR_TOPIC — cost if wrong: Phase 3 adds 1 code line, 3 test lines and a §6 note; until then a failed regenerate_topics would leave the run in WAITING_FOR_TOPIC (advisory A3).
49:Task 5: parked — [0] the report's live outputs (Step 0 signature, Step 7 table list, alembic current/check/round trip, 11 passed, ruff/mypy, 813 passed) were not re-run by the reviewer — Ruling: no gap; adjudicator re-ran them via `docker compose run --rm tools` with a read-only /app bind (override file in scratchpad, PYTHONDONTWRITEBYTECODE=1, no pytest/ruff/mypy caches): dbos 3.0.0 signature `(system_database_url: str, *, schema: str = 'dbos', application_role: Optional[str] = None) -> None`; `13 [...]` list identical to Step 7; dev DB `alembic current` = `0001 (head)` and `alembic check` = "No new upgrade operations detected." (exit 0); tests/db 11 passed; full suite 813 passed (also under -W error); "All checks passed!", "34 files already formatted", "no issues found in 19 source files", env.py "no issues found in 1 source file"; mdcopilot_blog_test dropped afterwards; all 16 delivered files byte-match the brief's code blocks (cmp) — cost if wrong: none, the evidence is direct; Tasks 6+ re-run the same gates.
50:Task 5: parked — [1] T1/T2 interfaces consumed by Task 5 (ids.uuid7, Settings.database_url, dbos_system_database_url, postgres_password) were not re-inspected — Ruling: no gap; read-only source check plus a runtime probe in the same read-only container (types and booleans only, no secret values printed): uuid7() returns uuid.UUID version 7 and is monotonic; database_url signature is `(self, database: str | None = None) -> URL`, driver postgresql+psycopg, default and override database names correct, str/repr mask the password; postgres_password is SecretStr; dbos_system_database_url is a str whose password round-trips through make_url; translate_connect_args gives user/dbname/host/port/password as `_admin_connect_kwargs` needs; the fixtures work end to end (813 passed) — cost if wrong: none, the evidence is direct.
51:Task 5: parked — [2] the report's cross-task arithmetic (Task 3's 13th logging test, 813 total) depends on Task 3's file outside the diff — Ruling: no gap; tests/unit/test_logs.py has 13 test functions (matching the Task 3 ledger line "13 logs + 11 problem responses"), and per-file runs give 4 + 47 + 13 + 11 + 60 + 405 + 262 + 11 = 813, equal to the brief's stated total and preflight's T5 running total — cost if wrong: none; only a count in the report, not code.
53:Task 6: parked — [0] ARCHITECTURE §19 checked only against the brief's paraphrase — Ruling: no gap; adjudicator read §19 (ARCHITECTURE.md:730-734) and the Phase 1 "Prompt registry" item (IMPLEMENTATION_PLAN.md:59) directly: layout backend/prompts/<agent>/<name>.v<N>.md, front matter name/version/agent/output/variables, Jinja2 StrictUndefined, hash + register in blog_prompt_versions, refuse a changed registered version without a bump — all present in registry.py; hashing the full file bytes is stricter than "text" and matches the plan's "hashes each file"; the startup refusal is wired by consumers (Task 8 cli sync-prompts, Task 11 worker startup calling sync_to_db); "active version is a setting" (rollback) is served here by get/render(version=) and its setting and recording (prompt_name/version/sha via RenderedPrompt) belong to Task 10 and later phases, not this task — cost if wrong: a later phase adds a version setting lookup at the call site; no Task 6 code changes.
54:Task 6: parked — [1] Task 5's PromptVersion model (columns, unique (name, version)) not visible in the diff — Ruling: no gap; db/models/llm.py:60-71 has name String(128), version int, agent String(64), sha256 String(64), body Text, front_matter dict→JSONB (base.py type_annotation_map) with UniqueConstraint uq_blog_prompt_versions_name_version; migration 0001 matches (version Integer NOT NULL, front_matter JSONB NOT NULL default '{}', same unique constraint, schema app); the 3 db tests pass against it — cost if wrong: none, the evidence is direct.
55:Task 6: parked — [2] Task 5's db_session fixture (create_savepoint pattern) not visible in the diff — Ruling: no gap; tests/conftest.py db_session opens engine.connect(), begins an outer transaction, binds AsyncSession(join_transaction_mode="create_savepoint", expire_on_commit=False), then closes and rolls back the outer transaction, so sync_to_db's commit only releases a savepoint and nothing survives the test; later committing CLI tests truncate blog_prompt_versions themselves (Task 8 TRUNCATE_CLI_SQL) — cost if wrong: none, the evidence is direct.
56:Task 6: parked — [3] reported RED/GREEN, ruff/mypy and 828 full-suite results not re-run by the reviewer — Ruling: no gap; adjudicator re-ran them via `docker compose run --rm tools` with the read-only /app bind (override in scratchpad, PYTHONDONTWRITEBYTECODE=1, no pytest/ruff/mypy caches): Step 7 "15 passed", full suite "828 passed" (also under -W error), "All checks passed!", "39 files already formatted", "Success: no issues found in 21 source files", mypy on the two test files clean; host find counts 39 and 21; all 5 delivered files byte-match the brief's code blocks (cmp); 12 unit + 3 db test functions; 813 + 15 = 828 matches preflight's running total; throwaway container removed, no project file changed — cost if wrong: none, the evidence is direct.
57:Task 6: parked — [4] downstream use of hello/echo v1 by Task 10's gateway test and Task 11's HELLO_SPEC not verifiable from the diff — Ruling: no gap for Task 6; task-10-brief renders prompt_name "hello/echo" with variables {"brand_name","topic"} via PromptRegistry.render and records prompt_sha=rendered.sha256, task-11-brief's HELLO_SPEC uses prompt_name "hello/echo" and passes the same two variables and calls sync_to_db at startup, task-8-brief uses from_directory/default_prompt_root/sync_to_db/PromptRegistryError, all with the signatures Task 6 ships; Task 14's runtime image keeps WORKDIR /app, installs editable (0 "--no-editable") and copies /app/prompts, so default_prompt_root resolves — cost if wrong: caught by Tasks 10/11 tests, which run against this exact file.
58:Task 6: parked — [5] frontend/package-lock.json flagged "changed or added" with content omitted — Ruling: false positive from package.sh; frontend/ exists in neither task-6-base nor task-6-r0 (nor the live tree; Task 13 creates it), and `cmp -s` on two missing files exits 2, which the script's `! cmp` treats as a change (the same line appears in review-task-1-r0.diff); backend/uv.lock is identical across the snapshots; diff -rq lists only the 5 Task 6 paths — cost if wrong: none for Task 6; the controller may want package.sh to test file existence before cmp so reviews stop flagging it.
64:Task 7: parked — cannot verify User.email has a UNIQUE constraint behind create_user's SAVEPOINT race claim — Ruling: confirmed, no change: db/models/auth.py:17 declares `String(320), unique=True` and migration 0001 line 92 creates `uq_users_email`, plus `ck_users_email_lowercase` (line 90), so uniqueness holds on the normalized value and a lost race raises IntegrityError inside begin_nested(), which becomes UserExists; spec ARCHITECTURE.md:602 "email unique" is met — cost if wrong: a concurrent duplicate insert would succeed silently (ruled out by the migration)
65:Task 7: parked — cannot verify login_attempts.created_at uses server now() and user_sessions expires_at/last_seen_at are timezone-aware — Ruling: confirmed, no change: CreatedAt mixin (db/base.py) uses server_default=func.now(), migration line 72 has `server_default now()`, and Base.type_annotation_map maps datetime to DateTime(timezone=True) (migration lines 220-221 timestamptz); matches spec ARCHITECTURE.md:598 "all timestamps are timestamptz" and the brief's database-clock rule — cost if wrong: naive/aware comparison errors in resolve_session and rate-limit windows off by the DB clock (ruled out)
66:Task 7: parked — cannot verify column widths behind the email[:320] / ip[:64] / user_agent[:400] truncation — Ruling: confirmed, no change: users.email and login_attempts.email are String(320), ip String(64) on both login_attempts and user_sessions, user_agent String(400) (models/auth.py:17,35,36,43,44; migration lines 69-70, 82, 222-223), so every slice matches its column; login_allowed not slicing email to 320 only affects emails over 320 characters, which cannot match a real account, and the IP limit still applies — cost if wrong: an over-long value would raise StringDataRightTruncation on flush (ruled out)
67:Task 7: parked — cannot verify AuditLog.details is JSONB and can store the dict audit() builds — Ruling: confirmed, no change: models/auth.py:57 `Mapped[dict[str, Any]]` resolves to JSONB via Base.type_annotation_map, migration line 104 is postgresql.JSONB, NOT NULL with server_default '{}'::jsonb, and audit() always passes a JSON-safe dict (never None); spec ARCHITECTURE.md:602 audit fields are covered — cost if wrong: audit rows fail to insert or details stored as text (ruled out)
69:Task 8: parked — [0] container-run evidence (tests/db 61, full suite 899, ruff/mypy, real `migrate` twice giving app|14 / dbos|13) was not re-run by the reviewer — Ruling: no gap; adjudicator re-ran it via `docker compose run --rm --no-deps tools` with a read-only /app bind (override in scratchpad, PYTHONDONTWRITEBYTECODE=1, no pytest/ruff/mypy caches): tests/db "61 passed" with the brief's per-file split (audit 2, cli 12, migrations 11, prompt_sync 3, rate_limit 6, seed 5, sessions 12, users 10); full suite "899 passed" (also under -W error); "All checks passed!", "57 files already formatted", "Success: no issues found in 31 source files"; read-only psql on the dev DB shows app|14, dbos|13, alembic 0001, blog_settings 1 row v1 active, brand 1 row v1 active, pillars A,B,C,D,E,NARRATIVE, prompts hello/echo@v1, users 0 and audit 0 (no accounts created); one more `cli migrate` exited 0 with the brief's exact second-run lines (settings_created=False brand_created=False pillars_created=0, sync-prompts: 0 inserted), no lowercase `postgresql` and no password in the output (checked in-container with grep -qF, value not printed), row counts unchanged; all 7 delivered files byte-match the brief's code blocks (cmp); no tools-run containers left; observation only: mypy on tests/db/test_cli.py reports 5 func-returns-value notes on the brief's `lambda s: order.append(...) or 1` lines, but every plan gate (Tasks 8, 9, 11, 12, 14) runs `mypy src` only and constraints scope mypy to src/ — cost if wrong: none, the evidence is direct.
70:Task 8: parked — [1] that click.exceptions.Exit is a RuntimeError (load-bearing for migrate_dbos's except clause and redaction) was checked from general knowledge — Ruling: no gap; in the image click is 8.5.0 and Exit.__mro__ is Exit, RuntimeError, Exception, BaseException, object; dbos 3.0.0's run_dbos_database_migrations (dbos.cli.migration) wraps SystemDatabase.create + run_migrations in `except Exception as e: click.echo(f"DBOS migrations failed: {e}"); raise click.exceptions.Exit(code=1)`, so every migration failure reaches the `except RuntimeError` branch (application_role is not passed, so the ungated grant path never runs); a real `cli migrate-dbos` with a wrong POSTGRES_PASSWORD override exited 1 and printed only `migrate-dbos: DBOS migrations failed: (psycopg.OperationalError) ... password authentication failed for user ...` to stderr plus a dbos WARNING naming the database, with 0 hits for the canary password, `postgresql` or `@db` — cost if wrong: none, the evidence is direct; a future click that drops RuntimeError would surface as an unredacted traceback, caught by test_migrate_dbos_failure_prints_the_redacted_cause, which raises the real click Exit.
71:Task 8: parked — [2] whether Settings.database_url()/T5 fixtures point only at mdcopilot_blog_test is outside the diff — Ruling: no gap; settings.py:226-244 builds database_url() from `database or self.postgres_db` and dbos_system_database_url is a @property over database_url(), both evaluated at call time, so conftest's `get_settings().model_copy(update={"postgres_db": "mdcopilot_blog_test", ...})` retargets both (model_copy skips validators, but neither value is stored); conftest's database_url fixture drops/creates only TEST_DB through the `postgres` maintenance DB, migrates it via settings.database_url(), and `_dbos_migrate` uses settings.database_url(TEST_DB) explicitly; every cli.main call in test_cli.py passes `settings=` (none falls back to get_settings()), and migrate/migrate-dbos tests stub the real Alembic/DBOS calls; after the suite the dev DB still has users 0/audit 0 and pg_database has no mdcopilot_blog_test (dropped at session end), and current_database() of the dev connection is not the test DB — meets the Global Constraint "tests use the disposable database mdcopilot_blog_test only" — cost if wrong: a CLI test would commit rows into the dev DB (ruled out by the post-suite counts).
76:Task 9: parked — [0] Task 7 auth primitives used by the api (resolve/create/revoke_session, revoke_user_sessions incl. keep_token, csrf_token_for, origin_allowed, tokens_match, SAFE_METHODS, login_allowed, record_login_attempt, authenticate, normalize_email, create_user, set_password, UserExists, MIN_PASSWORD_LENGTH, hash_token, ABSOLUTE_TIMEOUT) are outside the diff — Ruling: no gap; adjudicator read auth/*.py and services/audit.py, which are byte-identical to task-7-r0 and task-9-base. Every name exists with the keyword-only signature that deps.py, auth.py and users.py call. revoke_user_sessions(db, user_id, *, now, keep_token=None) skips hash_token(keep_token), so an admin who changes their own password keeps only the current session. UserExists subclasses ValueError and users.py catches it first (409 before 422). MIN_PASSWORD_LENGTH=12 matches UserCreate, and ABSOLUTE_TIMEOUT=12h gives Max-Age=43200. The 30-minute idle timeout, 12-hour absolute limit, hashed token, HMAC CSRF token and Sec-Fetch-Site/Origin order match ARCHITECTURE §17. Task 7's 54 tests and Task 9's 84 api tests pass on this code — cost if wrong: none, the evidence is direct.
77:Task 9: parked — [1] dbos 3.0.0 DBOSClient async methods (enqueue_async, cancel_workflow_async, list_workflow_steps_async, list_workflows_async, fork_workflow_async, destroy) were taken on the DB test's word — Ruling: no gap; checked in the image. dbos 3.0.0 ships py.typed, so mypy strict already checked the calls. inspect gives DBOSClient(*, system_database_url, dbos_system_schema, application_name, lazy, ...), enqueue_async(options, *args), cancel_workflow_async(workflow_id, *, cancel_children=False), list_workflow_steps_async(workflow_id, *, load_output=True, ...), list_workflows_async(*, workflow_ids, load_input, load_output, ...), fork_workflow_async(workflow_id, start_step, *, application_version, queue_name, ...) and destroy(). EnqueueOptions has the keys queue_name, workflow_name, workflow_id, app_version and workflow_timeout. StepInfo's keys are the ones the wrapper reads (error: Optional[Exception]). dbos's list_workflow_steps sets error=None when load_output=False, which confirms why list_steps keeps the default. The 3 DB tests pass against the real dbos schema. Task 9 exercises the real fork call only through the stub, but Task 11's worker tests call fork_from_step for real (task-11-brief lines 1390 and 1424) — cost if wrong: a fork keyword mismatch would surface in Task 11 (ruled out by signature and mypy).
78:Task 9: parked — [2] Role membership and permissions_for are outside the diff — Ruling: no gap; domain/enums.py and domain/rbac.py are byte-identical to task-7-r0 and task-9-base. Role has exactly 5 members (viewer, editor, reviewer, publisher, admin), ROLE_PERMISSIONS equals the ARCHITECTURE §17 table (re-read here; already checked against the spec in Task 4's ruling), and blog.settings is admin-only. test_rbac_routes.py collects 27 tests (4 routes × 5 roles, 4 anonymous, 3 structural). The matrix test uses permissions_for as its oracle, and test_only_admin_holds_settings_permission pins that oracle to the spec — cost if wrong: none.
79:Task 9: parked — [3] errors.py behaviour is outside the diff — Ruling: no gap; errors.py is byte-identical to task-7-r0 and task-9-base. problem_response builds {type, title, status, instance[, detail]} with media_type application/problem+json. install_problem_handlers maps ProblemError, Starlette HTTPException (404 gives the title "Not Found") and RequestValidationError (422 "Request validation failed", input stripped). ProblemError(403, "Forbidden", "missing permission …") yields the detail the tests read. The exact content-type assertions pass, and the real uvicorn smoke test printed `401 application/problem+json {...}` — cost if wrong: none.
80:Task 9: parked — [4] db/engine.py and the User model are outside the diff — Ruling: no gap; engine.py, models/auth.py and base.py are byte-identical to task-7-r0 and task-9-base. make_engine(url, **kw) calls create_async_engine with pool_pre_ping=True. make_sessionmaker uses expire_on_commit=False, so UserOut.model_validate after commit never lazy-loads. User has id, email (String(320), unique), display_name, role (String(32)), password_hash, is_active, last_login_at, created_at (server default, returned on insert) and updated_at, which covers every UserOut field. The lifespan test reads current_database() = mdcopilot_blog_test through make_sessionmaker — cost if wrong: none.
81:Task 9: parked — [5] the report's test, lint and type output was not re-run by the reviewer — Ruling: no gap; adjudicator re-ran it with `docker compose run --rm tools` and a read-only /app bind (override in scratchpad, PYTHONDONTWRITEBYTECODE=1, no pytest/ruff/mypy caches). Results: "994 passed" (also under -W error); the Task 9 files gave 106 passed with the brief's per-file split (auth 11, csrf 14, health 7, problem_errors 11, rbac 27, settings 4, users 10, workflow client 13 + 3, schemas 6); "All checks passed!", "78 files already formatted", "Success: no issues found in 43 source files"; the Step 26 uvicorn smoke test printed the three expected lines; and the live tree equals task-9-r0 (diff -rq is empty). Process note: one adjudicator command wrongly piped `docker compose config` output to host python3 (a read-only parse; nothing was written and no secret was printed); every gate above ran in the container — cost if wrong: none.
83:Task 10: parked — [0] gateway.py:297-328 duplicates the 11-keyword `_record_agent_attempt` call in the `except ADVANCE_ERRORS` and `except Exception` branches (plan-mandated) — Ruling: the plan stands; gateway.py byte-matches the brief's code block (cmp), and spec §7.1 only requires one row per attempt, advancing on the listed errors and re-raising the rest, which the code does and 23 gateway tests pin; the duplication is two adjacent calls to one helper whose parameters are all keyword-only with no defaults, so a new required field makes `mypy src` (a gate) flag any call site that omits it; no later brief greps these lines, but folding them now would make gateway.py drift from the plan text that later checks compare with cmp — cost if wrong: a later phase folds the two branches into one (about 10 lines, no behaviour change).
84:Task 10: parked — [1] alignment with ARCHITECTURE §7.1/§14/§18/§20 checked only against the brief — Ruling: no Phase 1 gap; adjudicator read the spec (ARCHITECTURE.md:286-348, 553-591, 684-703, 736-751; IMPLEMENTATION_PLAN.md:55-58). Met: run() walks the route itself (no FallbackModel) and advances on ModelAPIError, UnexpectedModelBehavior and httpx/httpx2 TransportError (covers timeouts and connection errors); one row per attempt with fallback_from; the recorder stores requested and served provider/model, params, input/output/cache/reasoning tokens, search_actions, latency, status, error class, cost_usd, price_version, prompt name/version/sha and the full CallContext incl. trace_id; the budget is checked before the first call, before every fallback and in search(); mock mode sets ALLOW_MODEL_REQUESTS=False and serves fixture search, and non-mock model/search/embed raise ProviderNotAvailable; grep finds no pydantic_ai/openai/google.genai/anthropic import outside llm/; the §14 protocol has key, capabilities, validate, publish(idempotency_key, as_draft) and find_existing, and NullPublisher only records; §20 FunctionModel route walking and 5xx/timeout fallback are tested. Plan interpretations and deferrals, all accepted: the §7.1 sketch (`inputs: BaseModel`, AgentSpec route_key/reasoning, ModelChoice params) is realised as variables + user_prompt with the route keyed by name (Task 11 depends on this); §14's `network: bool` member is exposed as `capabilities().network` (BLOG_PUBLISHING_ENABLED gating is Phase 7); the per-provider concurrency limit and an embed() budget check only matter once real providers exist (Phase 2); §7's "anthropic entries skipped without a key" holds in real mode, while mock mode returns routes unfiltered (the brief's documented choice; no real call is possible) — cost if wrong: Phase 2 adds a concurrency limiter and an embed budget check; Phase 7 adds a `network` attribute (about 2 lines) or reads capabilities().network; mock-mode route walks include an anthropic step that real mode would skip.
85:Task 10: parked — [2] Task 2/4/5/6 interfaces consumed (Settings fields, LlmCall/BlogRun, enums, PromptRegistry/RenderedPrompt/default_prompt_root) not in the diff — Ruling: no gap; settings.py:72-139 has mock_mode bool, the 3 keys as SecretStr|None, all 9 *_route RouteList fields, embedding_model str, embedding_dimensions int ge=1, max_cost_per_run_usd Decimal ge=0, and routes.py's getter map covers all 9 non-HELLO AgentName members; CallKind AGENT/SEARCH/EMBEDDING, CallStatus OK/ERROR and PublicationStatus.PUBLISHED exist; db/models/llm.py LlmCall has every column the recorder writes, and the widths fit (prompt_sha 64 = sha256 hex; trace_id 32 = new_trace_id uuid4().hex; fallback_from 200 >= provider + ':' + model(128)); BlogRun is exported from db.models; prompts/registry.py has RenderedPrompt(name, version, sha256, text), render(name, variables, version=None) and default_prompt_root; `mypy src` strict is clean on 54 files and the full suite gives 1052 passed (read-only container) — cost if wrong: none, the evidence is direct.
86:Task 10: parked — [3] the brief's claim that the suite passes without tests/unit/__init__.py and tests/db/__init__.py was left unverified by the implementer — Ruling: no gap, claim verified; in a --rm tools container with /app mounted read-only, a full copy of backend/ differing only by those two files gave "73 passed" (Task 6 + Task 10 set); tests/ has no duplicate test basenames; both files in the live tree byte-match task-10-base, and the live tree equals task-10-r0. Process note for the controller: the implementer temporarily deleted and restored two files outside its file list; the restore is exact — cost if wrong: none.
87:Task 10: parked — [4] a single compose run failed parsing .env line 9 (instruction-shaped text in a variable name) — Ruling: not a Task 10 code gap, but the OWNER must check .env; metadata only (no content read or printed): mode 600, 113 lines, 6362 bytes, modified 2026-09-17 12:48:03, which is inside the Task 10 implementer's window (report written 12:49:23) although the implementer says it never wrote .env; `docker compose config --quiet` now exits 0, and every compose run in this adjudication succeeded; Task 10 does not depend on .env values (tests derive Settings with model_copy; 1052 passed). Owner action: open .env yourself, confirm who changed it at 12:48 and that no unexpected text or entries remain — cost if wrong: an unexplained write to the secrets file goes unexamined; Settings ignores unknown variables (extra="ignore"), so the app is unaffected either way.
88:Task 10: parked — [5] the report's mypy/ruff and test outputs were not re-run by the reviewer — Ruling: no gap; adjudicator re-ran them with `docker compose run --rm tools` and a read-only /app bind (override in scratchpad, PYTHONDONTWRITEBYTECODE=1, no pytest/ruff/mypy caches): "58 passed"; "73 passed", also under -W error; "All checks passed!", "94 files already formatted", "Success: no issues found in 54 source files"; mypy --strict on the 5 test files clean; "ALLOW_MODEL_REQUESTS True" and "imports ok"; full suite "1052 passed" (994 + 58, matching preflight's running total); all 18 delivered files byte-match the brief's code blocks (cmp); no t10-adj containers left and mdcopilot_blog_test dropped — cost if wrong: none, the evidence is direct.
92:Task 11: parked — [0] cannot verify domain/state_machine.py run transition table from the diff — Ruling: no gap; read the table: every move hello.py makes is legal (QUEUED->RESEARCHING->TOPICS_READY->PRODUCING->SUCCEEDED; QUEUED/RESEARCHING/TOPICS_READY/PRODUCING->FAILED; SUCCEEDED/FAILED/CANCELLED->QUEUED for forks; QUEUED->PRODUCING for a fork that starts at hello.finish), and CANCELLED->anything-but-QUEUED raises InvalidTransition (a ValueError), which is exactly what _advance, mark_failed_step and _close_cancelled_workflow rely on; the table matches ARCHITECTURE §6; test_set_run_status_follows_the_state_machine and the 1087-test suite pass. WAITING_FOR_TOPIC->FAILED is not in the table, but hello never enters that state (a Phase 3 matter, not Task 11) — cost if wrong: a fork or cancel path raises instead of stopping cleanly, which the hello tests would show.
93:Task 11: parked — [1] cannot verify the Settings fields Task 11 reads — Ruling: no gap; settings.py has all of them, with safe defaults and validation: production_timeout_minutes int ge=1 default 30 (spec §5.5 "produce 30 min"), mock_step_delay_seconds float ge=0 default 0, scheduler_enabled default False (safety default), agent_enabled default True, daily_run_time default "07:00" with an HH:MM regex check (so _slot_time's split(":") is safe), timezone default Asia/Kolkata checked with ZoneInfo (so the ZoneInfo calls in schedules.py cannot fail), dbos_system_database_url a property built from database_url(), worker_executor_id "worker-1", app_version "0.1.0", log_level normalised and checked. The brief's "Settings fields used" line not listing production_timeout_minutes is only paperwork from ruling R-T11; the code uses the field correctly — cost if wrong: a bad env value crashes the worker at startup rather than misbehaving silently.
94:Task 11: parked — [2] cannot verify LLMGateway.run and MockModelFactory naming (Task 10) — Ruling: no gap; gateway.py's LLMGateway.run(spec, *, variables, user_prompt, ctx) matches echo_step's call; MockModelFactory.build returns FunctionModel(model_name=f"mock:{spec.name.value}") with AgentName.HELLO = "hello", and the gateway records model_served from response.model_name, which _close_step_row copies into blog_agent_runs.model, so "mock:hello" is correct; test_hello_pipeline_runs_end_to_end passed — cost if wrong: only the model name on a step row would be wrong, and the end-to-end test checks it.
95:Task 11: parked — [3] cannot verify WorkflowClient semantics (Task 9) — Ruling: no gap; client.py's enqueue(workflow_name, queue_name, workflow_id, args, timeout_seconds) sets app_version and maps timeout_seconds to workflow_timeout; cancel calls cancel_workflow_async; list_steps returns StepView; status returns the DBOS status string or None; fork_from_step picks the latest function_id for the step name and forks with application_version and queue_name (spec §5.5, Spike S1 "pass queue_name"). This matches how hello.py and the tests use it, and the tests (including both fork tests) and the Step 26 crash-recovery check passed — cost if wrong: fork/cancel tests would fail, and they do not.
96:Task 11: parked — [4] cannot verify db/models.py constraints that schedules.py and tracking.py depend on — Ruling: no gap; runs.py defines Index("uq_blog_runs_daily_date", "run_date", unique=True, postgresql_where=text("kind = 'daily'")) and UniqueConstraint("dbos_workflow_id", "dbos_step_id", name="uq_blog_agent_runs_wf_step"), and migration 0001 creates both the same way. create_daily_run_step's on_conflict_do_nothing(index_elements=["run_date"], index_where=text("kind = 'daily'")) matches the partial unique index (Postgres rejects an ON CONFLICT clause with no matching index even when no row conflicts, so the passing insert tests prove the match), and _open_step_row's constraint= name matches — cost if wrong: duplicate daily runs or broken retry counting, which the schedules and tracking tests (tries=2 on recovery) would show.
99:Task 12: parked — [0] api.schemas (ManualRunRequest, RunOut, AttemptOut, StepOut, RunDetail, Page[T]) and ApiModel config were outside the diff — Ruling: no gap; schemas.py is byte-identical in task-9-r0, task-11-r0, task-12-base and task-12-r0. ApiModel sets alias_generator=to_camel, validate_by_name, validate_by_alias, serialize_by_alias, extra="forbid" and from_attributes. So `RunDetail(**to_run_out(run).model_dump(by_alias=False), ...)` passes exactly RunOut's 10 snake_case field names, all of which are RunDetail fields (extra="forbid" holds), and FastAPI emits camelCase. Every required output field is non-null in the models: BlogRun.params (default dict / '{}'), cost_usd (default 0), and created_at (a server default, which SQLAlchemy 2.0.54's eager_defaults="auto" fetches through RETURNING; the real-sessionmaker integration test reads it after commit). AgentRun tries, input_tokens, output_tokens and cost_usd all have defaults. The 422 cases test ManualRunRequest's bounds (wordCount 300–3000, topic ≤300, pillar enum, extra forbidden). Plan interpretation accepted: spec §16's `{date, ...}` sketch is served as an optional `runDate` that defaults to the local date in settings.timezone. That matches blog_runs.run_date and the camelCase rule, and the only client (Task 13) takes its types from OpenAPI. test_run_detail_shape_and_ordering passes on re-run — cost if wrong: renaming one alias, if the owner wants the literal key `date`.
100:Task 12: parked — [1] api.deps (Principal, require_permission, SessionDep, SettingsDep, WorkflowClientDep, enforce_csrf) was outside the diff — Ruling: no gap; deps.py is byte-identical since task-9-r0. Principal is a frozen dataclass (user_id, email, display_name, role, permissions, session_token), matching the service test's constructor. require_permission raises ProblemError(403, "Forbidden", f"missing permission {permission.value}"), and Permission.GENERATE is "blog.generate", the exact detail the tests read. SessionDep yields one session per request; FastAPI caches the dependency, so current_principal's commit and the cancel route's FOR UPDATE read share that session, and error paths close it with a rollback. SettingsDep and WorkflowClientDep read app.state first. create_app installs enforce_csrf app-wide. It skips safe methods, returns early when there is no cookie (so the route answers 401), and raises 403 "CSRF token missing or invalid" on a bad token, as spec §17 and the brief's error table require. Every runs route depends on CanView or CanGenerate, and test_rbac_routes (walks ROUTERS) gives 27 passed with the new routes — cost if wrong: none.
101:Task 12: parked — [2] domain.state_machine semantics (Entity, InvalidTransition, require_transition) were outside the diff — Ruling: no gap; state_machine.py is byte-identical since task-9-r0 and was checked against spec §6 in the Task 11 ruling. The run table lets QUEUED, RESEARCHING, TOPICS_READY, WAITING_FOR_TOPIC and PRODUCING move to CANCELLED. SUCCEEDED, FAILED and CANCELLED may move only to QUEUED. A same-state move is an allowed no-op, and QUEUED->FAILED (the enqueue-failure path) is legal. So cancel_run's require_transition raises InvalidTransition only for SUCCEEDED and FAILED. The router maps that to 409 "Run cannot be cancelled" using exc.current, which the class sets. A CANCELLED run returns before the check, with no client call and no audit. This matches spec §5.5 "Cancel: cancel_workflow; run -> CANCELLED" — cost if wrong: none; the parametrised 409 tests and the service tests cover both terminal states.
102:Task 12: parked — [3] workflows.client (WorkflowClientProtocol, FakeWorkflowClient, EnqueueCall, enqueue_error raised before recording) was outside the diff — Ruling: no gap; client.py is byte-identical since task-9-r0. EnqueueCall is a frozen dataclass with fields in the order (workflow_name, queue_name, workflow_id, args, timeout_seconds), so the test's equality check is exact. FakeWorkflowClient is a dataclass with `enqueued` and `cancelled` lists, and enqueue raises enqueue_error before appending (client.py:167-169). The real WorkflowClient.enqueue stamps app_version and maps timeout_seconds to workflow_timeout, and cancel calls cancel_workflow_async. Checked in the image (dbos 3.0.0): cancel_workflow_async calls cancel_workflows([id]), which is one UPDATE ... WHERE workflow_uuid IN (...) AND status NOT IN (SUCCESS, ERROR). Cancelling an id that does not exist yet (a daily run between create_daily_run_step and the child's start) is therefore a silent no-op, not a 503. The child then finds the run CANCELLED: tracking.py:102 records its attempt as CANCELLED, and hello.py's _advance catches InvalidTransition. The integration test drives the real client to SUCCESS — cost if wrong: none in Phase 1 (the daily schedule is paused).
103:Task 12: parked — [4] the reported test counts and lint/mypy output were not re-run by the reviewer — Ruling: no gap; adjudicator re-ran them with `docker compose run --rm tools` and a read-only /app bind (override in scratchpad, PYTHONDONTWRITEBYTECODE=1, no pytest/ruff/mypy caches). Step 14 gave "43 passed", also with -e BLOG_AGENT_ENABLED=false. Step 15 gave "27 passed" for the RBAC guards and "1130 passed" for the full suite under -W error. Step 16 gave "All checks passed!", "111 files already formatted" and "Success: no issues found in 62 source files". The implementer's one transient integration failure did not recur: 10 more Step 14 runs each gave 43 passed, and 5 runs of tests/workflows each gave 36 passed. The brief's fallback (a background-thread DBOS launch in dbos_runtime) therefore stays unused; the brief applies it only if the failure recurs more than rarely. All 5 delivered files byte-match the brief's code blocks (cmp). app.py has exactly the two runs_routes lines, and the live backend equals task-12-r0. No t12adj containers are left, and mdcopilot_blog_test was dropped — cost if wrong: an intermittent DBOS-timing failure would reappear in Task 14's acceptance run, and the brief's recorded fallback applies.
106:Task 13: parked — [0] cannot verify build/test/lint/typecheck/Docker/smoke evidence from the diff — Ruling: no gap; re-ran independently in throwaway node:24-alpine containers against a read-only copy of frontend/: build exit 0, "2137 modules transformed", JS 529.67 kB (167.52 kB gzip), CSS 47.75 kB, the expected >500 kB warning; vitest 6 files / 35 tests with the brief's per-file counts (7/7/5/3/5/8); eslint . and tsc -b print nothing; nginx -t passes in mdcopilot-blog-web:prod; prod smoke gives 200 + Cache-Control no-cache for /drafts, 502 for /api/healthz with nginx still up; dev smoke gives VITE v8.3.0 ready, a Network 172.x line and <title>MDCopilot Blog</title>. The JS bundle is 529,676 bytes in host dist, the prod image and my run; CSS is 47.4–47.8 kB across build environments, all "about 47 kB". 2137 vs 2127 modules comes from the lucide-react 1.47.0 registry drift in [1] and changes no outcome — cost if wrong: none found; a regression would still surface in Task 14's acceptance run.
107:Task 13: parked — [1] cannot verify scaffold/shadcn-generated files match real CLI output — Ruling: no gap; in throwaway containers with the project mounted read-only, a fresh `npm create vite@9.2.1 frontend -- --template react-ts --eslint` gives byte-identical README.md, .gitignore, tsconfig.node.json and public/favicon.svg, and a fresh `shadcn@4.21.0 add` of the 11 components on a copy gives byte-identical ui/*.tsx and leaves package.json unchanged; components.json is radix-nova/neutral/lucide, utils.ts is `export { cn } from "cn"`, index.css is 129 lines starting with the four imports. Only drift: lucide-react ^1.47.0 (lock 1.47.0) vs the brief's ^1.46.0, which the brief's own command resolves today; hand-pinning would break the brief's "do not hand-edit" rule for installer output — cost if wrong: a one-line package.json/lock version difference against the brief's npm ls list, no behaviour change (build/tests/lint/typecheck green).
108:Task 13: parked — [2] cannot verify eslint.config.js was written exactly once under R-T13 — Ruling: no gap; the file is byte-identical to the brief's Step 3 block and differs from a fresh create-vite 9.2.1 file only by the two owner-approved changes ('.vitest' in globalIgnores, the src/components/ui/**/*.tsx override); its birth, modify and change times are all 13:45:54 (created once, never rewritten); .frontend-scaffold/ no longer exists; and ECC 2.2.1 config-protection.js allows only first-time creation and blocks later Write/Edit of eslint.config.js — cost if wrong: none to the code, only the process record.
109:Task 13: parked — [3] cannot verify Docker image state and smoke-container cleanup — Ruling: no gap; docker image ls shows mdcopilot-blog-web:dev (615 MB, 13:52) and :prod (102 MB, 13:53), matching the brief's ~614/~102 MB and kept for Task 14; docker ps -a shows no mdcopilot-blog-web-smoke/-devsmoke or other leftover throwaway containers (only mdcopilot-blog-db-1 and unrelated stacks); my own t13-adjudicator-* verification containers were --rm or removed by name, and none remain — cost if wrong: negligible; Task 14's compose build rebuilds the images anyway.
111:Task 13: minor (deferred): task-13-report.md ("Ruling R-T13 applied" section) — Ruling R-T13 (rulings.md §3.3 / Task 13) names the temporary scaffold directory literally as `mdcopilot-blog/.frontend-scaffold-tmp/frontend`; the report describes using `.frontend-scaffold/frontend` instead (no `-tmp` suffix). The directory was d
114:- Owner change 1 (2026-09-17): owner asked to remove the 12-character password minimum to keep a 9-character admin password. Ruling: minimum becomes 1 (empty still refused), tests rewritten in place, acceptance script and LOCAL_DEVELOPMENT updated; plan file left as historical record — cost: weaker admin credential; acceptable only while the app is bound to 127.0.0.1; owner warned the password was pasted in chat.
144:- Final review: Ruling: residual "Task 10 parked [4]: unexplained .env write at 12:48" is closed. The 12:48 write was the owner adding OPENAI_API_KEY (164 chars) and GEMINI_API_KEY (39 chars): +203 bytes, exactly 6159→6362. Line 9 is GEMINI_API_KEY with a 39-char value; .env has 0 malformed lines; `docker compose config --quiet` passes. The single parse failure happened during the owner's edit — cost if wrong: none; the check used only metadata (names and lengths), no values.
145:- Ruling: Phase 1 workspace NOT deleted, and superpowers:finishing-a-development-branch not used — this project has no git history, so the ledger, reports and snapshots are the only record — cost: disk space under .superpowers/ (git-ignored).
